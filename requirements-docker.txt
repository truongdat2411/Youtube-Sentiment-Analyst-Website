# Dependencies cho image Docker backend (API).
# PyTorch CPU duoc cai rieng trong Dockerfile (index download.pytorch.org/whl/cpu)
# de tranh ban CUDA + cudnn hang GB tu PyPI (de gay pip exit code 2: het d dia / timeout).

fastapi
uvicorn[standard]
python-jose[cryptography]
bcrypt>=4.0.1
email-validator
sqlalchemy
asyncpg
psycopg[binary]
alembic
pydantic-settings
python-dotenv
google-api-python-client
tenacity
transformers
mlflow==2.13.0
httpx
pandas
